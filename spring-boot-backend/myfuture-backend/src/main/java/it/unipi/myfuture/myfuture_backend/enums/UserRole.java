package it.unipi.myfuture.myfuture_backend.enums;

/**
 * Defines the role of a user inside the system. Used to distinguish permissions and accessible APIs.
 */
public enum UserRole {
    user,
    admin
}
